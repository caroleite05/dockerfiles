#### Please, note that this repository only contains a mirror to the `{{original_image}}` Docker image, providing guidance on how to use it. This way, we can keep a copy of this Docker image in our account for the sake of repeatability. Read below for further information.

# {{tool_name}}

The original Dockerfile is available [at {{source_dockerfile}}]({{dockerfile_url}}).

# Using the {{tool_name}} image in Linux

You should adapt and run the following command:
`{{docker_command}}`

In this command, you should replace:
- `/your/data/dir` to point to the directory that contains {{directory_description}}
- `{{input_file}}` to the actual name of your {{input_description}}
- `{{output_file}}` to the actual name of your {{output_description}}

To see the [{{tool_name}}]({{tool_url_help}}) help, just run:
`{{help_command}}`